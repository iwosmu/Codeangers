# copy to .env  --  never commit .env
GEMINI_API_KEY=

# true until the real prompts exist; every endpoint then answers from fixtures/
MOCK_ONLY=true

# verify these names in Google AI Studio before the event -- they rotate
MODEL_EXTRACT=gemini-3.5-flash-lite
MODEL_REASON=gemini-3.8-flash

CORS_ORIGINS=http://localhost:5173
